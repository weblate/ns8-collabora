#
# This systemd unit starts a collabora instance using Podman.
# Most parts of this file come from podman-generate-systemd.
#
[Unit]
Description=collabora server

[Service]
Environment=PODMAN_SYSTEMD_UNIT=%n
EnvironmentFile=%S/state/environment
WorkingDirectory=%S/state
Restart=always
ExecStartPre=/bin/rm -f %t/collabora.pid %t/collabora.ctr-id
ExecStart=/usr/bin/podman run \
    --detach \
    --conmon-pidfile=%t/collabora.pid \
    --cidfile=%t/collabora.ctr-id \
    --cgroups=no-conmon \
    --replace --name=%N \
    --publish=127.0.0.1:${TCP_PORT}:8080 \
    ${ECHO_SERVER_IMAGE}
ExecStop=/usr/bin/podman stop --ignore --cidfile %t/collabora.ctr-id -t 10
ExecStopPost=/usr/bin/podman rm --ignore -f --cidfile %t/collabora.ctr-id
PIDFile=%t/collabora.pid
Type=forking

[Install]
WantedBy=default.target
